name: CI
on:
  pull_request: {}
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  ci:
    uses: beCyborg/jadlis-hub/.github/workflows/plugin-ci.yml@main
    with:
      mode: plugin
      forbid-mermaid: true
    secrets: inherit
